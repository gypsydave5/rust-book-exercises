fn main() {
    // constants
    const MAX_POINTS: u32 = 100_000;

    // mutable variable
    let mut x = 5;
    println!("The value of x is: {}", x);

    // reassignment
    x = 6;
    println!("The value of x is: {}", x);
}
